CREATE DATABASE "Infotech" WITH ENCODING 'UTF8' TEMPLATE template0;

CREATE TABLE roles (
    id integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name text NOT NULL UNIQUE
);

CREATE TABLE users (
    id integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    role_id integer NOT NULL REFERENCES roles (id),
    full_name text NOT NULL,
    phone text NOT NULL,
    login text NOT NULL UNIQUE,
    password text NOT NULL
);

CREATE TABLE requests (
    id integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    start_date date NOT NULL,
    tech_type text NOT NULL,
    tech_model text NOT NULL,
    problem text NOT NULL,
    status text NOT NULL,
    completion_date date,
    repair_parts text,
    master_id integer REFERENCES users (id),
    client_id integer NOT NULL REFERENCES users (id)
);

CREATE TABLE comments (
    id integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    message text NOT NULL,
    master_id integer NOT NULL REFERENCES users (id),
    request_id integer NOT NULL REFERENCES requests (id)
);

INSERT INTO roles (id, name) VALUES
    (1, 'Менеджер'),
    (2, 'Техник'),
    (3, 'Оператор'),
    (4, 'Заказчик');

INSERT INTO users (id, role_id, full_name, phone, login, password) VALUES
    (1, 1, 'Минаева Алиса Тимофеевна', '89210563128', 'login1', 'pass1'),
    (2, 2, 'Воробьев Фёдор Алексеевич', '89535078985', 'login2', 'pass2'),
    (3, 2, 'Захарова Алёна Андреевна', '89210673849', 'login3', 'pass3'),
    (4, 3, 'Гусева Василиса Дмитриевна', '89990563748', 'login4', 'pass4'),
    (5, 3, 'Миронов Даниэль Львович', '89994563847', 'login5', 'pass5'),
    (6, 4, 'Белов Роман Добрынич', '89219567849', 'login6', 'pass6'),
    (7, 4, 'Кузин Михаил Родионович', '89219567841', 'login7', 'pass7'),
    (8, 4, 'Ковалева Софья Владимировна', '89219567842', 'login8', 'pass8'),
    (9, 4, 'Глухова Вероника Владимировна', '89219567843', 'login9', 'pass9'),
    (10, 2, 'Князев Арсений Андреевич', '89219567844', 'login10', 'pass10');

INSERT INTO requests (id, start_date, tech_type, tech_model, problem, status, completion_date, repair_parts, master_id, client_id) VALUES
    (1, '2023-06-06', 'Компьютер', 'RDOR GAMING RAGE H290', 'Выключается после 10 минут работы', 'В процессе ремонта', NULL, NULL, 2, 7),
    (2, '2023-05-05', 'Ноутбук', 'ASUS VivoBook Pro 15 M6500QH-HN034 синий', 'Сильно шумит и греется', 'В процессе ремонта', NULL, NULL, 3, 8),
    (3, '2022-07-07', 'Мышка', 'ARDOR GAMING Phantom PRO', 'Перестало работать колёсико', 'Готова к выдаче', '2023-01-01', NULL, 3, 9),
    (4, '2023-08-02', 'Клавиатура', 'Dark Project KD83A', 'Сломались некоторые клавиши', 'Новая заявка', NULL, NULL, NULL, 8),
    (5, '2023-08-02', 'Ноутбук', 'ASUS ROG Strix G15 G513RW-HQ177 серый', 'Не загружается система', 'Новая заявка', NULL, NULL, NULL, 9);

INSERT INTO comments (id, message, master_id, request_id) VALUES
    (1, 'Всё сделаем!', 2, 1),
    (2, 'Не переживаейте, починим.', 3, 2),
    (3, 'Не переживаейте, починим.', 3, 3);

SELECT setval(pg_get_serial_sequence('roles', 'id'), (SELECT MAX(id) FROM roles));
SELECT setval(pg_get_serial_sequence('users', 'id'), (SELECT MAX(id) FROM users));
SELECT setval(pg_get_serial_sequence('requests', 'id'), (SELECT MAX(id) FROM requests));
SELECT setval(pg_get_serial_sequence('comments', 'id'), (SELECT MAX(id) FROM comments));
